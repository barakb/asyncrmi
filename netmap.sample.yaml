---
rules:
    - match: {host: .*}
      filters: [tls compress]
    - match: {host: 192.168.2.106}
      filters: [drop]
    - match: {host: 192.168.2.106, port : 2021}
      filters: [tls compress]
tls:
    keystoreFile: .keystore
    keystorePass: changeit
    clientAuth: no

...
